<UserControl x:Class="DemoApp.UserControls.ControleListagemDiretorio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DemoApp.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Diretório"  
               Background="Green" 
               Grid.Row="0" 
               Grid.Column="0" 
               Grid.ColumnSpan="2"/>

    <TextBox Text="{Binding Diretorio, Mode=TwoWay}" 
             Grid.Row="1" Grid.Column="0"/>

    <Button Content="Listar" Grid.Row="1" 
            Grid.Column="1" 
            Click="btnListar_Click"/>

    <ListBox ItemsSource="{Binding ItensDiretorio}" 
             SelectedItem="{Binding ItemSelecionado, Mode=TwoWay}" 
             Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"/>

  </Grid>
</UserControl>
